// There are 7 types of operators in C 

// Arithmetic operator
// Relational operator
// Logical operator
// Assignment operator
// Conditional operator (ternary conditional operator)
// Unary operator
// Bitwise operator

// Other Operators
// // Increment and Decrement Operators
//     int t = 5;
//     int u = ++t; // increment operator (t is incremented first, then assigned to u)
//     int v = t--; // decrement operator (t is assigned to v first, then decremented)

//     // sizeof Operator
//     int size = sizeof(int); // returns the size in bytes of the datatype or variable

//     // Comma Operator
//     int w, x, y, z;
//     w = 1, x = 2, y = 3, z = 4; // multiple expressions separated by commas; value of the last expression is the result

//     // Address and Indirection Operators
//     int var = 10;
//     int *ptr = &var; // address of operator (gets the memory address of a variable)
//     int value = *ptr; // indirection or dereference operator (gets the value stored at the memory address)

//     // Cast Operator
//     float f = 3.14;
//     int i = (int)f; // cast operator converts the value of one data type to another

